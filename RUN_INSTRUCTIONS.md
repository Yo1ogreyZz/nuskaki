# NUS Kaki 运行说明

## 📋 前置要求

1. **安装 Ollama**
   - 下载地址: https://ollama.ai/download
   - 安装后确保 Ollama 服务正在运行

2. **下载模型**
   ```bash
   ollama pull llama3.2:3b
   ```

3. **确认 Ollama 运行**
   ```bash
   ollama serve
   ```
   或者直接运行 `ollama run llama3.2:3b` 测试模型是否可用

---

## 🚀 如何运行项目

### 方式一：使用两个终端（推荐）

#### 终端 1 - 启动后端服务器
```bash
npm run server
```
你应该看到：
```
=================================
🚀 后端服务器已启动!
📍 运行地址: http://localhost:3000
🤖 使用模型: llama3.2:3b
=================================
```

#### 终端 2 - 启动前端
```bash
npm run serve
```
前端将运行在 http://localhost:8080

### 方式二：一键启动（需要安装 concurrently）

如果你想同时启动前后端，可以安装 concurrently：
```bash
npm install --save-dev concurrently
```

然后在 package.json 中添加：
```json
"dev": "concurrently \"npm run server\" \"npm run serve\""
```

然后只需运行：
```bash
npm run dev
```

---

## 🧪 测试步骤

1. 确保 Ollama 服务正在运行（可以单独打开一个终端运行 `ollama serve`）

2. 启动后端服务器：
   ```bash
   npm run server
   ```

3. 启动前端（新开一个终端）：
   ```bash
   npm run serve
   ```

4. 打开浏览器访问 http://localhost:8080

5. 在聊天框中输入消息，例如：
   - "What is NUS?"
   - "Tell me about campus facilities"
   - "你好，介绍一下自己"

---

## ⚠️ 常见问题

### 问题 1: "Ollama 服务未启动"
**解决方法**：
- 打开新的终端运行 `ollama serve`
- 或者确保 Ollama 应用程序正在后台运行

### 问题 2: 后端连接失败
**检查项**：
- 后端服务器是否在运行（应该在 http://localhost:3000）
- 端口 3000 是否被占用
- 查看后端终端是否有错误信息

### 问题 3: 模型响应很慢
**说明**：
- llama3.2:3b 是本地模型，首次加载需要时间
- 响应速度取决于你的电脑配置
- 如果太慢，可以考虑使用更小的模型，如 `llama3.2:1b`

### 问题 4: 端口冲突
如果端口 3000 被占用，修改 `server.js` 中的 PORT：
```javascript
const PORT = 3001; // 改成其他端口
```

同时修改 `ChatBot.vue` 中的 API 地址：
```javascript
const response = await axios.post('http://localhost:3001/api/chat', {
```

---

## 📁 项目架构

```
nuskaki/
├── server.js              # 后端服务器（Express + Ollama）
├── src/
│   ├── components/
│   │   └── ChatBot.vue    # 前端聊天组件
│   └── plugins/
│       └── axios.js       # Axios 配置
├── package.json
└── RUN_INSTRUCTIONS.md    # 本文档
```

---

## 🔧 技术栈

- **前端**: Vue 3, Axios
- **后端**: Node.js, Express
- **AI模型**: Ollama (llama3.2:3b)

---

## 📝 API 接口

### POST /api/chat
发送消息给 AI

**请求**：
```json
{
  "prompt": "你的问题"
}
```

**响应**：
```json
{
  "success": true,
  "response": "AI的回复"
}
```

### GET /api/health
健康检查

**响应**：
```json
{
  "status": "ok",
  "message": "服务器运行正常"
}
```

---

## 🎉 完成！

现在你的 NUS Kaki 聊天机器人已经是一个真正的 AI 助手了！

